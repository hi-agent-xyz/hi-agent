<?xml version="1.0" encoding="utf-8"?>
<!-- Nothing this app stores should ride a backup or a device transfer off this
     handset. The credential is a Keystore-wrapped secret that would not decrypt
     elsewhere anyway; the roster is small enough to re-pair. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="file" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="file" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
    </device-transfer>
</data-extraction-rules>
